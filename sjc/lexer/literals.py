"""
Handles type inference for literals.

Should contain a function that takes a string argument eg "12.0" or "'hello'" (with quotes inside) and give it the correct type.

NOTE this will need to handle more complex type inference for objects and inferring the type of their keys.
"""
